# =============================================
# DEVELOPMENT ENVIRONMENT
# =============================================

# Spotify API Configuration
# Get your credentials from https://developer.spotify.com/dashboard
SPOTIFY_CLIENT_ID=your_spotify_client_id_here
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret_here
SPOTIFY_REDIRECT_URI=http://localhost:8080/api/auth/callback

# Server Configuration
PORT=8080
NODE_ENV=development

# Session Secret (REQUIRED - Use a strong random string of at least 32 characters)
# Generate one using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your_random_session_secret_here_at_least_32_characters_long

# CORS Configuration
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:5173

# =============================================
# PRODUCTION ENVIRONMENT (Render)
# =============================================
# Render에서 배포 시 다음 환경 변수를 설정하세요:
#
# NODE_ENV=production
# PORT=10000 (Render 자동 설정)
# SPOTIFY_CLIENT_ID=<Spotify Dashboard에서 발급받은 Production Client ID>
# SPOTIFY_CLIENT_SECRET=<Spotify Dashboard에서 발급받은 Production Client Secret>
# SPOTIFY_REDIRECT_URI=https://your-app-name.onrender.com/api/auth/callback
# SESSION_SECRET=<Render에서 자동 생성 또는 수동 입력>
# ALLOWED_ORIGINS=https://your-frontend-domain.com
#
# 주의: Spotify Dashboard(https://developer.spotify.com/dashboard)에서
# Redirect URI를 추가해야 합니다:
# https://your-app-name.onrender.com/api/auth/callback
