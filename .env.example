# =============================================
# DEVELOPMENT ENVIRONMENT
# =============================================

# Spotify API Configuration
# Get your credentials from https://developer.spotify.com/dashboard
SPOTIFY_CLIENT_ID=your_spotify_client_id_here
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret_here
SPOTIFY_REDIRECT_URI=http://localhost:8080/api/auth/callback

# Server Configuration
PORT=8080
NODE_ENV=development

# Session Secret (REQUIRED - Use a strong random string of at least 32 characters)
# Generate one using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your_random_session_secret_here_at_least_32_characters_long

# CORS Configuration
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:5173

# =============================================
# PRODUCTION ENVIRONMENT (Render)
# =============================================
# Render에서 배포 시 다음 환경 변수를 설정하세요:
#
# NODE_ENV=production
# PORT=10000 (Render 자동 설정)
# SPOTIFY_CLIENT_ID=<동일한 Client ID 사용>
# SPOTIFY_CLIENT_SECRET=<동일한 Client Secret 사용>
# SPOTIFY_REDIRECT_URI=https://your-actual-render-url.onrender.com/api/auth/callback
# SESSION_SECRET=<새로운 랜덤 문자열 생성 - 개발용과 다르게>
# ALLOWED_ORIGINS=https://your-frontend-vercel-url.vercel.app
#
# 예시:
# SPOTIFY_REDIRECT_URI=https://vibelink-server.onrender.com/api/auth/callback
# ALLOWED_ORIGINS=https://vibelink-app.vercel.app
#
# 중요: Spotify Dashboard(https://developer.spotify.com/dashboard)에
# 프로덕션 Redirect URI를 추가로 등록해야 합니다:
# - http://127.0.0.1:8080/api/auth/callback (개발용)
# - https://your-actual-render-url.onrender.com/api/auth/callback (프로덕션)
